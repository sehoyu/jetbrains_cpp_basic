정수 배열을 복사하는 함수를 작성하세요.

```c++
int* copy(int* p, size_t size);
```

이 함수는 배열의 시작 포인터와 배열의 크기를 인수로 받아야 하며,  
배열 복사를 위한 충분한 메모리를 할당한 후,  
배열 내용을 복사하여 이 메모리를 초기화해야 합니다.  
만약 배열의 크기가 0이면, 함수는 null 포인터를 반환해야 합니다.