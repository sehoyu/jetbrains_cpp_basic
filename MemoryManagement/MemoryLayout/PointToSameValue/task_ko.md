함수 `pointToSameValue`를 구현하세요. 이 함수는 두 개의 포인터를 입력으로 받아, 포인터가 가리키는 변수들의 값이 동일한지 확인합니다.

```c++
bool pointToSameValue(int* a, int* b);
```

포인터 중 하나가 null인 경우를 처리하도록 하세요. 두 포인터 모두 null인 경우, 함수는 `true`를 반환해야 합니다.